mutation updateCommentOnEntry($id: ID!, $text: String!, $traineeId: ID!, $commentId: ID!) {
  updateCommentOnEntry(id: $id, text: $text, traineeId: $traineeId, commentId: $commentId) {
    commentable {
      ... on Entry {
        id
        comments {
          id
          text
          user {
            id
            firstName
            lastName
          }
          published
        }
      }
    }
  }
}
