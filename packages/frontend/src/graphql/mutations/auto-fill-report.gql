mutation AutoFillReport($reportId: ID!) {
  autoFillReport(reportId: $reportId) {
    id
    days {
      status
      id
      entries {
        id
        text
        time
        orderId
      }
    }
  }
}
