mutation CreateTimetable($input: TimetableInput!) {
  createTimetable(input: $input) {
    id
    timetables {
      id
      traineeId
      title
      dateStart
      dateEnd
      entries {
        id
        day
        timeStart
        timeEnd
        subject
        teacher
        room
        notes
      }
    }
  }
}
