mutation updateTimetable($input: TimetableUpdateInput!) {
  updateTimetable(input: $input) {
    id
    timetables {
      id
      traineeId
      title
      dateStart
      dateEnd
      entries {
        id
        day
        timeStart
        timeEnd
        subject
        teacher
        room
        notes
      }
    }
  }
}
