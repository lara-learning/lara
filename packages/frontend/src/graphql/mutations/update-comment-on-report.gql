mutation updateCommentOnReport($id: ID!, $text: String!, $traineeId: ID!, $commentId: ID!) {
  updateCommentOnReport(id: $id, text: $text, traineeId: $traineeId, commentId: $commentId) {
    commentable {
      ... on Report {
        id
        comments {
          id
          text
          user {
            id
            firstName
            lastName
          }
          published
        }
      }
    }
  }
}
