query Config {
  config {
    minWorkDayMinutes
    maxWorkDayMinutes
    expectedWorkDayMinutes

    minEducationDayMinutes
    maxEducationDayMinutes

    maxEntryMinutes

    maxPeriodYearsCount

    finishedWeekDayCount
  }
}
